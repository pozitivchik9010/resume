<div>
    <p>{{title}}</p>

    {{#each options}}

    <div>
        <label class="form-check-label">
            <input class="form-check-input" type="checkbox" name="delivery_type">
            {{value}}
        </label>
    </div>
    {{/each}}
</div>